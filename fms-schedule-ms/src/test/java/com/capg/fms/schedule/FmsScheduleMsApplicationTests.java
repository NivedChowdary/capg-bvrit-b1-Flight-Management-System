package com.capg.fms.schedule;

import static org.junit.jupiter.api.Assertions.assertTrue;

import java.time.LocalDateTime;
import java.time.Month;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import com.capg.fms.schedule.model.Schedule;
import com.capg.fms.schedule.model.ScheduledFlight;
import com.capg.fms.schedule.service.ScheduleServiceImpl;

@SpringBootTest
class FmsScheduleMsApplicationTests {

	/*
	 * Schedule schedule = new Schedule(101, "HYD", "MUM", LocalDateTime.of(2020,
	 * 04, 28, 03, 45, 00), LocalDateTime.of(2020, 04, 29, 02, 10, 00));
	 * ScheduledFlight scheduledFlight = new ScheduledFlight(101, 200, 143,
	 * schedule);
	 * 
	 * @Autowired ScheduleServiceImpl service;
	 * 
	 * @Test void contextLoads() { }
	 * 
	 * @Test void validateAddScheduledFlight(ScheduledFlight scheduledFlight) {
	 * 
	 * 
	 * 
	 * }
	 * 
	 * @Test void validateViewScheduledFlight(int scheduledFlightId) {
	 * 
	 * assertTrue(scheduledFlightId>0);
	 * 
	 * }
	 * 
	 * @Test void validateDeleteScheduledFlight(int scheduledFlightId) {
	 * 
	 * assertTrue(scheduledFlightId>0);
	 * 
	 * }
	 */

}
